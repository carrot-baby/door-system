server:
  port: 8080


spring:
  application:
    name: send-to-Accept26
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  datasource:
    url: jdbc:mysql://localhost:3306/metrics?serverTimezone=UTC&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  aop:
    auto: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false

